import { type Metadata } from 'next';

// export const metadata: Metadata = {
//   title: 'Pricing',
//   description: 'Manage pricing for customers',
// };

export default function PricingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 